#!/bin/bash

#module purge
module load cmake
module load boost

#      -D Boost_LIBRARY_DIRS:PATH="$BOOST_DIR"/libs \
#      -D Boost_INCLUDE_DIRS:PATH="$BOOST_DIR" \

#export CMAKE_PREFIX_PATH="${SCRATCH}/trilinos/dev/master/pgi/include/"

cmake \
    -D CMAKE_PREFIX_PATH:STRING="${SCRATCH}/trilinos/dev/master/pgi/ ${CMAKE_PERFIX_PATH}" \
    -D BOOST_INCLUDEDIR:PATH="${BOOST_DIR}" \
    -D Boost_LIBRARYDIR:PATH="${BOOST_DIR}/boost" \
    ../../source
