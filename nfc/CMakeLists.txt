INCLUDE_DIRECTORIES(${Nosh_SOURCE_DIR}/src/)
INCLUDE_DIRECTORIES(
  SYSTEM
  ${Trilinos_INCLUDE_DIRS}
  ${Trilinos_TPL_INCLUDE_DIRS}
  ${EIGEN3_INCLUDE_DIR}
  )

SET(MY_EXECUTABLE "main")
ADD_EXECUTABLE(${MY_EXECUTABLE} "main.cpp")
TARGET_LINK_LIBRARIES(${MY_EXECUTABLE} "nosh")
